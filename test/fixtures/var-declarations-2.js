export default function demo (input): number {
  let a : Array = input
  for(let b : string of a) {
    let c : string = b
    if(c == '1')
      var x : number = parseFloat(c)
  }
  if(x != null)
    return x
}
